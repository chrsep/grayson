import React, { FC } from "react"

const LogoStandaloneBlack: FC<{ className: string }> = ({ className }) => (
  <img className={className} src="/logo-black.svg" alt="Sekitarmu" />
)

export default LogoStandaloneBlack
